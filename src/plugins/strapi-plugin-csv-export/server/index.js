'use strict';

// Carregar módulos JavaScript diretamente
const controllers = require('./src/controllers');
const routes = require('./src/routes');
const services = require('./src/services');

module.exports = {
  controllers,
  routes,
  services,
};

